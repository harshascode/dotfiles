###backend = "glx";
#backend = "xrender";
#vsync = true;
###glx-use-copysubbuffer-mesa = true;

###glx-copy-from-front = true;
###glx-swap-method = 2;
#xrender-sync = true;
#xrender-sync-fence = true;

#fade-in-step = 0;
#fade-out-step = 0;
#shadow = false;





backend = "xrender";

# Disable vsync for potential performance gains, especially if you're not gaming.
vsync = true;

# Disable all animations and fading effects.
fading = false;
fade-in-step = 1;
fade-out-step = 1;
fade-delta = 1;

# Disable shadows completely.
shadow = false;

# Disable blur effects for a performance boost.
blur-background = false;

# Optional: Disable any transparency rules you may have.
# If you have a line like "opacity = 0.9;" anywhere, remove or comment it out.
# Set window opacity to 1.0 (100%) for maximum performance.
# active-opacity = 1.0;
# inactive-opacity = 1.0;